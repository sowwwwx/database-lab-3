1 - Function returns a value and can be used inside sql queries. 
Procedure is executed using 'call and doesnt have to return a value.
2 - A trigger cant be executed manually. It runs automatically with insert, update, delete. Trigger function contains logic executed by this trigger.
3 - Business logic keeps same rules for usage of database. Can improve performance by optimizing. But on the other hand can be complecated to maintain and migrate to other dbms.